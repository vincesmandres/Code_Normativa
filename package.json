{
  "name": "codenormative",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix apps/web run dev -- --host 0.0.0.0",
    "build": "npm --prefix apps/web run build",
    "preview": "npm --prefix apps/web run preview -- --host 0.0.0.0",
    "postinstall": "npm --prefix apps/web install"
  }
}
